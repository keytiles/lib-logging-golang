loggers:
  root:
    level: info
    handlers:
      - stdout_json

# These are appenders config
handlers:
  # This handler logs to STDOUT in structured JSON format
  stdout_json:
    level: debug
    encoding: json
    outputPaths:
      - stdout