# Logging (`kt_logging`) — v2.2

## Meta

- Generated / last modified: 2026-08-08 13:14
- This doc was generated by AI agent
- Package: `github.com/keytiles/lib-logging-golang/v2/pkg/kt_logging`
- Covers the library as of the **2.2.x** line (see `CHANGELOG.md`)
- Active plan: [`development-plans/v2.2.0/enhance-LoggerRegistryInMemory-v2.2-plan.md`](../development-plans/v2.2.0/enhance-LoggerRegistryInMemory-v2.2-plan.md)

## What changed vs previous

Inherited from [`logging-v2.1.md`](logging-v2.1.md) (self-contained below). New in **2.2.0**:

- Nil `*Logger` / nil-backed `LogEvent` are no-ops (no panic); inspectors treat nil as silent
- Bounded on-demand logger registry (FIFO by creation order):
  - Loggers from config (and lazy/hard-fallback `root`) are **pinned** forever
  - Names created by `GetLogger` / `With` that were not in config are cached up to **`loggerMaxCacheSize`** (default **3000**; omit or `0` = default; negative rejected at init)
  - When the cache is full, the **oldest** on-demand name (creation order) is dropped (logging still works — next get recreates it)
- Prefer stable logger names still; put per-request data in labels

---

## Panic policy

- **Startup may fail hard** — `InitFromConfig` returning an error (or the app panicking on that error) is acceptable. Bad config should stop the process during boot.
- **Runtime must not panic** — after startup, `GetLogger` / `With` and log emit (`Info`, `Warn`, `WithLabel`, …) must not panic. Lazy default init and missing-`root` recovery use safe fallbacks. A nil `*Logger` receiver (or `LogEvent` with a nil logger) is also safe: emit methods no-op; `IsSilent()` is true.

---

## TLDR

`kt_logging` is a thin, config-driven wrapper around [zap](https://pkg.go.dev/go.uber.org/zap). You:

1. Load a Python-style YAML/JSON config (`loggers` + `handlers`) via `InitFromConfig`
2. Optionally set **global labels** attached to every event
3. Obtain a named `Logger` with `GetLogger` / `With`
4. Log with `Printf`-style methods; add per-event labels via `WithLabel` / `WithLabels` → `LogEvent`

If you never call `InitFromConfig`, the first `GetLogger`/`With` creates a default **root** logger that writes JSON to stdout at info level. That lazy path is panic-free (hard fallback root if default construction fails).

---

## Starting points

Where to look when reading or changing this package:

- **Package entry / registry** — `pkg/kt_logging/ktlogging.go`  
  `InitFromConfig`, `GetLogger` / `With`, global labels, hierarchy resolution, default config, zap handler wiring
- **Config models & file parse** — `pkg/kt_logging/config.go`  
  `ConfigModel`, `LoggerConfigModel`, `HandlerConfigModel`, `RollingFileModel`
- **Logger API** — `pkg/kt_logging/logger.go`  
  Levels, filtering, `Info`/`Warn`/…, `WithLabel(s)`, `Is*Enabled` / `IsSilent`
- **Per-event label builder** — `pkg/kt_logging/log_event.go`  
  Short-lived `LogEvent` (value type) that collects labels then fires the log
- **Labels** — `pkg/kt_logging/label.go`  
  `StringLabel` / `IntLabel` / `FloatLabel` / `BoolLabel` and conversion to `zap.Field`
- **Naming constant** — `pkg/kt_logging/common.go` (`PACKAGE_NAME`)
- **Runnable example** — `example/usage_example.go` + `example/log-config.yaml`
- **Unit tests** — `tests/kt_logging/`
- **Benchmarks** — `tests/kt_logging_bench/` (summary script: `./tests/kt_logging_bench/run-benchmarks.sh`)
- **Quick consumer overview** — `README.md`

Import path:

```go
import "github.com/keytiles/lib-logging-golang/v2/pkg/kt_logging"
```

---

## How the pieces relate

```text
                    ┌─────────────────────────┐
                    │  YAML / JSON config     │
                    │  loggers + handlers     │
                    └───────────┬─────────────┘
                                │ InitFromConfig
                                ▼
┌──────────────┐      ┌─────────────────────────┐
│ GlobalLabels │─────▶│  package logger registry │
│ (all events) │      │  map[name]*Logger        │
└──────────────┘      └───────────┬─────────────┘
                                  │ GetLogger / With
                                  ▼
                         ┌────────────────┐
                         │    Logger      │  name, level, handlers
                         │                │
                         │ WithLabel(s)   │──▶ LogEvent (temp labels)
                         │ Info/Warn/...  │──▶ log() ──▶ each zap.Logger
                         └────────────────┘
                                  │
                                  ▼
                         ┌────────────────┐
                         │   Handlers     │  (zap cores / outputs)
                         │ stdout, file,  │
                         │ rolling file   │
                         └────────────────┘
```

Relations in words:

- **Config → Handlers → Loggers**  
  Init builds one underlying `*zap.Logger` per named handler, then each named logger gets a map of those handlers by name. Logger config must reference existing handlers. A **`root`** logger is mandatory in config.
- **Logger names are hierarchical**  
  Dot-separated names (e.g. `controller.something`) resolve by walking parents; if a name is missing, a **clone** of the nearest ancestor (or `root`) is registered under that name as an **on-demand** cache entry (same level + handlers, different `name` field on events), subject to `loggerMaxCacheSize`.
- **Labels attach in two scopes**  
  - Global: `SetGlobalLabels` / `GetGlobalLabels` — every event. Both copy: treat `Get` as a snapshot; always `Set` to publish changes (do not rely on mutating a previously returned slice).  
  - Per event: `Logger.WithLabel` / `WithLabels` → `LogEvent` — only that call chain  
  Every emitted event also gets a `logger` string field with the logger’s name.
- **Filtering is layered**  
  Logger level first (`None` / empty handlers → silent). Zap’s own handler level still applies on the wire. `Is*Enabled()` / `IsSilent()` reflect logger level **and** whether any handlers are attached.

---

## Typical usage flow

1. `kt_logging.InitFromConfig("./log-config.yaml")` (or `.json`)
2. Optionally `SetGlobalLabels([...])`
3. `logger := kt_logging.GetLogger("main")` or `kt_logging.With("main")`
4. `logger.Info("hello %v", x)` or `logger.WithLabel(...).Warn("...")`
5. Use `IsDebugEnabled()` (etc.) when building expensive messages

Message formatting uses `fmt.Sprintf` style. Formatting runs only after the logger decides the event is not filtered out (and has handlers).

---

## Log levels

Ordered from most restrictive to most verbose:

- `NoneLevel` (`none` / `off` in config) — logger emits nothing
- `ErrorLevel` (`error`)
- `WarningLevel` (`warning` / `warn`)
- `InfoLevel` (`info`)
- `DebugLevel` (`debug`)

Config level strings are case-insensitive. A log event is emitted only if `logger.level >= event level`.

---

## Configuration

Python-style: two maps — **loggers** and **handlers** — plus optional top-level **`loggerMaxCacheSize`**. See `example/log-config.yaml`.

### loggerMaxCacheSize

- Caps how many **on-demand** logger names (not listed under `loggers:`) are kept in memory
- Config loggers are always kept for the process lifetime
- When over the limit, the oldest on-demand name (creation order / FIFO) is dropped from the cache (recreated on next `GetLogger` / `With` if needed)
- Omit or `0` → default `3000` (`DefaultLoggerMaxCacheSize`); negative values fail `InitFromConfig`

### Loggers

Each entry (map key = logger name):

- `level` — see above; `none`/`off` for silence by level
- `handlers` — list of handler names; empty list ⇒ no output (`IsSilent()` true even if level is high)

`root` must be defined.

### Handlers

Each entry:

- `level` — zap atomic level for that output
- `encoding` — `json` or `console`
- Either:
  - `outputPaths` — e.g. `stdout`, or a file path (standard zap build), **or**
  - `rollingFile` — lumberjack rotation (`file`, `maxSizeMb`, `maxAgeDays`, `maxBackups`, `compress`)  
    **Cannot** combine `rollingFile` with `outputPaths` on the same handler.

Encoder keys used: `message`, `level`, `time` (RFC3339 nano). Caller and stacktrace are disabled.

### Default config (no `InitFromConfig`)

On first logger access without prior init:

- Logger `root`: level `info`, handler `stdout_json`
- Handler `stdout_json`: JSON to `stdout`, level `info`
- On-demand cache max: `DefaultLoggerMaxCacheSize` (3000)

### Supported file extensions

- `.yaml`
- `.json`

Anything else fails at parse time.

---

## Core types & APIs

### Package-level

- `InitFromConfig(cfgPath string) error`
- `GetLogger(name string) *Logger` — create-or-get; hierarchical fallback; on-demand names FIFO-cached by creation order
- `With(name string) *Logger` — alias of `GetLogger` for fluent style
- `SetGlobalLabels` / `GetGlobalLabels` — thread-safe; get/set copy (snapshot semantics)
- `DefaultLoggerMaxCacheSize` — default for `loggerMaxCacheSize` (3000)
- `PACKAGE_NAME` — `keytiles.lib.logging.kt_logging`

### `Logger`

- Identity / config: `GetName()`, `GetLevel()`, `GetHandlers()` (handlers map is a shallow copy)
- Log: `Log`, `Debug`, `Info`, `Warn`, `Error`
- Per-event labels: `WithLabel`, `WithLabels` → `LogEvent`
- Gates: `IsErrorEnabled`, `IsWarningEnabled`, `IsInfoEnabled`, `IsDebugEnabled`, `IsSilent`
- Nil-safe: a nil `*Logger` does not panic; emit methods no-op; `IsSilent()` is true

### `LogEvent`

Value type (stack-friendly). Collects labels with `WithLabel` / `WithLabels`, then fires with `Debug` / `Info` / `Warn` / `Error`. Labels apply only to that event. Emit is a no-op if the underlying logger is nil.

### `Label`

Atomic types only (string, bool, int64, float64) — no nested objects/arrays as label values (serialize complex data to a JSON string if needed). Constructors: `StringLabel`, `IntLabel`, `FloatLabel`, `BoolLabel`. Getters available since 2.1.0.

---

## Hierarchy behavior (important)

`GetLogger("a.b.c")`:

1. If `a.b.c` exists in the registry → return it
2. Else recurse to parent `a.b`, then `a`, then `root`
3. Clone that ancestor’s level + handlers, set `name` to the requested name, register as **on-demand** (may evict oldest extras), return

So `controller.something` with only `controller` in config inherits `controller`’s level and outputs, but events carry `logger: "controller.something"`.

Registry access uses an `RWMutex`: read lock on cache hit; write lock on miss / create / `InitFromConfig` swap.

### On-demand cache vs config loggers

- Loggers listed under `loggers:` stay in memory for the life of the process (**pinned**).
- If you call `GetLogger` / `With` with a name that is **not** in the config, the library creates a logger that inherits from its parent (hierarchy) and **caches** it.
- `loggerMaxCacheSize` limits how many of those **extra** (non-config) names are kept. When the limit is reached, the **oldest** extra names (creation order / FIFO) are removed from the cache. Logging still works — the next `GetLogger` for that name creates it again.

Prefer a small set of stable names and put per-request data in **labels**, not in dynamic logger names.

---

## Benchmarks

Unit tests: `tests/kt_logging/`.  
Benchmarks (separate package): `tests/kt_logging_bench/`.

```bash
go test ./tests/kt_logging/                                    # functionality only
./tests/kt_logging_bench/run-benchmarks.sh                     # emit benches + summary table
```

Handler-style emit benches use the YAMLs under `tests/kt_logging_bench/testdata/`. Most emit benches **cache** the logger once; `StdoutJson_WithEachOp` calls `With("main")` every iteration so you can compare registry lookup cost vs cached emit.

---

## Known limitations

- **Rolling files on Windows** — lumberjack can hit “file is being used by another process”; not reliable on Windows. Prefer non-rolling outputs there (see `CHANGELOG.md` release 2.0.0).
- Handlers are zap loggers; logger-level filtering happens in this package first, then zap may filter again by handler level.
- On-demand registry entries may be evicted under `loggerMaxCacheSize`; do not rely on pointer identity for non-config logger names across eviction.
